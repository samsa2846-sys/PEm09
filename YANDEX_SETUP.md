# 🚀 Настройка с Yandex Cloud API

## Преимущества Yandex API

- ✅ **Работает без VPN** - доступ из России
- ✅ **Российские платежи** - карты МИР, СБП
- ✅ **Быстрая регистрация** - без блокировок
- ✅ **Бесплатный грант** - 1000₽ на старте
- ✅ **Низкая цена** - дешевле OpenAI

---

## 📋 Что нужно для работы

### 1. Yandex GPT (основная модель)
- Генерация ответов
- Анализ технических вопросов
- **Цена:** ~0.3₽ за 1000 токенов

### 2. Yandex SpeechKit (опционально)
- STT - распознавание речи
- TTS - синтез речи
- **Цена:** ~0.2₽ за 15 секунд аудио

### 3. Yandex Vision (опционально)
- Анализ изображений/чертежей
- **Цена:** ~1₽ за изображение

---

## 🔑 Получение API ключей

### Шаг 1: Регистрация в Yandex Cloud

1. Перейдите на https://console.cloud.yandex.ru
2. Войдите через Яндекс ID или зарегистрируйтесь
3. Создайте платёжный аккаунт (нужна карта, но списаний не будет)
4. Получите **грант 1000₽** на первые 60 дней

### Шаг 2: Создание папки (Folder)

1. В консоли Yandex Cloud нажмите **"Создать папку"**
2. Введите имя: `pem09-technical-bot`
3. Скопируйте **Folder ID** (например: `b1g2345abcdefghijk`)

### Шаг 3: Создание API ключа

1. Перейдите в **"IAM"** → **"Сервисные аккаунты"**
2. Нажмите **"Создать сервисный аккаунт"**
3. Имя: `technical-bot-sa`
4. Роли:
   - `ai.languageModels.user` - для YandexGPT
   - `ai.speechkit-stt.user` - для распознавания речи (опционально)
   - `ai.speechkit-tts.user` - для синтеза речи (опционально)
   - `ai.vision.user` - для распознавания изображений (опционально)
5. Создайте **API ключ**:
   - Откройте созданный аккаунт
   - Вкладка **"API ключи"** → **"Создать ключ"**
   - Скопируйте ключ (выглядит как `AQVN...`)
   - ⚠️ Сохраните сразу - больше не покажется!

### Шаг 4: Активация сервисов

Перейдите в каждый сервис и активируйте:

1. **Yandex GPT:** https://console.cloud.yandex.ru/folders/{folder-id}/yandexgpt
2. **SpeechKit:** https://console.cloud.yandex.ru/folders/{folder-id}/speechkit
3. **Vision:** https://console.cloud.yandex.ru/folders/{folder-id}/vision

---

## ⚙️ Настройка проекта

### 1. Установите дополнительные зависимости

Обновите `requirements.txt`:

```txt
# Existing dependencies
pyTelegramBotAPI>=4.14.0
python-dotenv>=1.0.0

# For PDF processing
pypdf>=3.17.0
PyPDF2>=3.0.1

# HTTP requests
requests>=2.31.0
aiohttp>=3.9.0
aiofiles>=23.2.1

# Testing
pytest>=8.2.0
pytest-asyncio>=0.23.0
```

Установите:
```bash
pip install -r requirements.txt
```

### 2. Создайте .env файл

```env
# Telegram Bot
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather

# Выбор API провайдера: yandex, openai, или proxyapi
API_PROVIDER=yandex

# Yandex Cloud Configuration
YANDEX_API_KEY=AQVN_ваш_api_ключ_из_yandex_cloud
YANDEX_FOLDER_ID=b1g_ваш_folder_id

# Yandex Voice (опционально, по умолчанию: alena)
# Доступные: alena, filipp, ermil, jane, omazh, zahar, dasha
YANDEX_TTS_VOICE=alena

# OpenAI (не нужен, если используете Yandex)
# OPENAI_API_KEY=sk-...
# USE_PROXYAPI=false

# Bot settings
BOT_MODE=rag
LOG_LEVEL=INFO
```

### 3. Обновите main.py

Файл `main.py` уже поддерживает оба API. При запуске автоматически определится провайдер из `.env`.

### 4. Проверьте конфигурацию

```bash
python -c "from config import API_PROVIDER, YANDEX_API_KEY; print(f'Provider: {API_PROVIDER}, Key: {YANDEX_API_KEY[:10]}...')"
```

Должно вывести:
```
Provider: yandex, Key: AQVN123456...
```

---

## 🚀 Запуск

```bash
python main.py
```

Вы должны увидеть:
```
INFO - Yandex GPT client initialized with model: yandexgpt-lite
INFO - Yandex SpeechKit client initialized
INFO - Found 1 documents, indexing...
INFO - Indexed 156 document chunks
INFO - Bot started: @ваш_бот
```

---

## 💬 Тестирование

В Telegram боте:

### Базовый тест
```
/start
/mode rag
Какой люк выбрать для магистральной дороги?
```

### Тест голосового режима
```
/mode voice
[Отправьте голосовое: "Расскажи про класс D400"]
```

### Тест анализа изображения
```
/mode vision
[Отправьте фото чертежа или документа]
```

---

## 💰 Стоимость использования

### Yandex GPT Lite (рекомендуется)
- **Input:** ~0.12₽ за 1000 токенов
- **Output:** ~0.24₽ за 1000 токенов
- **Один запрос:** ~0.05-0.15₽

### Yandex GPT (полная версия)
- **Input:** ~0.30₽ за 1000 токенов
- **Output:** ~0.60₽ за 1000 токенов
- **Один запрос:** ~0.15-0.40₽

### SpeechKit
- **STT:** ~0.20₽ за 15 секунд
- **TTS:** ~0.20₽ за 1000 символов

### Vision
- ~1₽ за изображение

### Итого для проекта:
- **Индексация каталога:** ~30-50₽ (один раз)
- **100 вопросов:** ~10-20₽
- **С голосом:** +5-10₽
- **Месяц активного использования:** 200-500₽

**Грант 1000₽ хватит на 2-3 месяца тестирования!**

---

## 🔄 Сравнение: Yandex vs OpenAI

| Параметр | Yandex Cloud | OpenAI/ProxyAPI |
|----------|--------------|-----------------|
| **VPN** | ❌ Не нужен | ✅ Нужен (кроме ProxyAPI) |
| **Регистрация** | ✅ Простая | ⚠️ Сложная (блокировки) |
| **Оплата** | 💳 Карты МИР, СБП | 💳 Только международные |
| **Бесплатный грант** | ✅ 1000₽ | ❌ Нет |
| **Цена текста** | 💰 0.12-0.30₽/1K токенов | 💰 ~2.5$ /1M (дороже) |
| **Качество GPT** | ⭐⭐⭐ Хорошее | ⭐⭐⭐⭐⭐ Отличное |
| **Голос (STT)** | ✅ Да, ~0.2₽/15сек | ✅ Да, дороже |
| **Голос (TTS)** | ✅ Да, ~0.2₽/1K символов | ✅ Да, дороже |
| **Vision** | ✅ Да, ~1₽/картинка | ✅ Да, дороже |
| **RAG эмбеддинги** | ⚠️ Нет (используем keyword) | ✅ Да (OpenAI Embeddings) |

**Рекомендация:**
- **Для России:** Yandex Cloud (проще и дешевле)
- **Для максимального качества:** OpenAI GPT-4o
- **Компромисс:** ProxyAPI (OpenAI через прокси)

---

## 🔧 Настройка моделей

### Изменить модель YandexGPT

В `.env` можно указать:
```env
# Легкая версия (быстрее, дешевле)
YANDEX_GPT_MODEL=yandexgpt-lite

# Полная версия (качественнее, дороже)
YANDEX_GPT_MODEL=yandexgpt
```

Или напрямую в `config.py`:
```python
YANDEX_GPT_MODEL = "yandexgpt-lite"  # или "yandexgpt"
```

### Доступные голоса для TTS

```env
YANDEX_TTS_VOICE=alena  # Женский (нейтральный)
# alena - Алёна (нейтральный женский)
# filipp - Филипп (мужской)
# ermil - Ермил (мужской)
# jane - Джейн (женский, эмоциональный)
# omazh - Омаж (женский)
# zahar - Захар (мужской)
# dasha - Даша (детский)
```

---

## 📊 Мониторинг использования

### 1. Консоль Yandex Cloud
Перейдите: https://console.cloud.yandex.ru/billing

Здесь вы увидите:
- Текущий баланс
- Использование гранта
- Детализацию по сервисам

### 2. Логи бота
В консоли отображаются все запросы:
```
INFO - Generating response with yandexgpt-lite
INFO - Audio transcribed: 45 characters
INFO - Speech generated: /path/to/audio.ogg
```

### 3. Yandex Monitoring
Автоматический мониторинг вызовов API:
https://console.cloud.yandex.ru/folders/{folder-id}/monitoring

---

## ⚠️ Устранение неполадок

### Ошибка: "Yandex GPT API error (403)"

**Причина:** Недостаточно прав или неверный API ключ

**Решение:**
1. Проверьте роли сервисного аккаунта
2. Убедитесь, что ключ скопирован полностью
3. Проверьте срок действия ключа

### Ошибка: "YANDEX_API_KEY is not set"

**Причина:** Не создан `.env` файл или неверное имя переменной

**Решение:**
```bash
# Проверьте наличие файла
ls -la .env

# Проверьте содержимое
cat .env | grep YANDEX
```

### Бот не отвечает на вопросы

**Причина:** Каталог не проиндексирован

**Решение:**
1. Проверьте наличие PDF: `ls data/documents/`
2. Проверьте индекс: `/stats` в боте
3. Пере индексируйте: удалите `data/simple_index/` и перезапустите

### Качество ответов хуже, чем с OpenAI

**Причина:** YandexGPT Lite менее мощная модель

**Решение:**
1. Используйте полную версию:
   ```env
   YANDEX_GPT_MODEL=yandexgpt
   ```
2. Или переключитесь на OpenAI:
   ```env
   API_PROVIDER=openai
   OPENAI_API_KEY=sk-...
   ```

---

## 📚 Полезные ссылки

- 📖 [Документация YandexGPT](https://cloud.yandex.ru/docs/yandexgpt/)
- 🎤 [Документация SpeechKit](https://cloud.yandex.ru/docs/speechkit/)
- 👁️ [Документация Vision](https://cloud.yandex.ru/docs/vision/)
- 💰 [Цены Yandex Cloud](https://cloud.yandex.ru/prices)
- 🆘 [Поддержка](https://console.cloud.yandex.ru/support)

---

## ✅ Чек-лист настройки

- [ ] Зарегистрирован в Yandex Cloud
- [ ] Получен грант 1000₽
- [ ] Создана папка (Folder)
- [ ] Создан сервисный аккаунт с ролями
- [ ] Получен API ключ
- [ ] Активированы сервисы (GPT, SpeechKit, Vision)
- [ ] Создан `.env` файл с ключами
- [ ] Установлены зависимости (`pip install -r requirements.txt`)
- [ ] Загружен PDF каталог в `data/documents/`
- [ ] Бот запущен и работает
- [ ] Протестированы основные функции

---

**Готово! Ваш бот работает на Yandex Cloud!** 🎉

**Экономия:** ~70% по сравнению с OpenAI + без VPN!


