# Технический консультант-каталог по люкам и дождеприемникам

Telegram-бот на базе Yandex Cloud API с функциями RAG для консультации по техническому каталогу продукции из высокопрочного чугуна (ВЧШГ-50).

## Возможности

- 📝 **Текстовый режим** — диалог с YandexGPT для общих вопросов
- 📚 **RAG режим** — поиск информации в техническом каталоге по люкам и дождеприемникам
- 🎤 **Голосовой режим** — распознавание речи и синтез голосовых ответов (Yandex SpeechKit)
- 📸 **Vision режим** — распознавание текста на изображениях (Yandex Vision OCR)

## Технологический стек

- **Backend**: Python 3.14, pyTelegramBotAPI (async)
- **AI Services**: Yandex Cloud (YandexGPT, SpeechKit, Vision)
- **RAG**: Keyword-based search (без векторных эмбеддингов)
- **База данных**: JSON файлы для индексации документов

## Быстрый старт

### 1. Клонирование репозитория

```bash
git clone https://github.com/samsa2846-sys/PEm09.git
cd PEm09
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Настройка переменных окружения

Создайте файл `.env` на основе `env.yandex.example`:

```bash
cp env.yandex.example .env
```

Заполните `.env` файл:

```env
# Telegram Bot Token
TELEGRAM_BOT_TOKEN=your_telegram_bot_token

# Yandex Cloud API
API_PROVIDER=yandex
YANDEX_API_KEY=your_yandex_api_key
YANDEX_FOLDER_ID=your_folder_id

# YandexGPT Model
YANDEX_GPT_MODEL=yandexgpt-lite

# Bot Settings
BOT_MODE=rag
LOG_LEVEL=INFO
```

### 4. Подготовка базы знаний

Поместите технический каталог PDF в папку `data/documents/`:

```bash
# Например:
cp Litlider_Katalog_VCHSHG_2025.pdf data/documents/
```

### 5. Запуск бота

```bash
python main.py
```

## Документация

- [Быстрый старт с Yandex](START_HERE_YANDEX.md) — инструкция для начинающих
- [Настройка Yandex Cloud](YANDEX_SETUP.md) — получение API ключей
- [Миграция с OpenAI](MIGRATION_TO_YANDEX.md) — если переходите с OpenAI
- [Руководство по RAG](RAG_GUIDE.md) — как работает база знаний
- [Настройка технического консультанта](SETUP_TECHNICAL_CONSULTANT.md) — специфика проекта

## Команды бота

- `/start` — запуск бота и приветствие
- `/help` — справка по командам
- `/mode` — переключение режимов (text/rag/voice/vision)
- `/stats` — статистика базы знаний
- `/reset` — очистка истории диалога

## Режимы работы

### Текстовый режим (`/mode text`)
Обычный диалог с YandexGPT для общих вопросов.

### RAG режим (`/mode rag`)
Поиск информации в техническом каталоге. Примеры вопросов:
- "Какой люк выбрать для магистральной дороги?"
- "Расшифруй маркировку ТМ(Д400)-2-7-9-60"
- "Чем отличается плавающий люк от обычного?"

### Голосовой режим (`/mode voice`)
Отправьте голосовое сообщение — бот распознает речь, обработает запрос и ответит голосом.

### Vision режим (`/mode vision`)
Отправьте фото документа — бот распознает текст (OCR) и классифицирует изображение.

## Архитектура

```
Пользователь (Telegram)
    ↓
handlers/ [text/voice/image/rag]
    ↓
services/router.py
    ↓
Yandex Cloud API (GPT/SpeechKit/Vision) + RAG (keyword search)
    ↓
Ответ в Telegram
```

Подробнее см. [ETAP4_ARKHITEKTURA.md](ETAP4_ARKHITEKTURA.md)

## Структура проекта

```
PEm09/
├── handlers/          # Обработчики Telegram команд и сообщений
├── services/          # Клиенты для Yandex Cloud API
├── rag/              # Модуль базы знаний (индексация, поиск)
├── utils/            # Вспомогательные утилиты
├── data/             # Документы и индексы
│   ├── documents/    # Исходные документы (PDF, TXT)
│   └── simple_index/ # Проиндексированная база знаний
├── main.py           # Точка входа
├── config.py         # Конфигурация
└── requirements.txt  # Зависимости
```

## Требования

- Python 3.10+
- Telegram Bot Token
- Yandex Cloud API Key и Folder ID
- Активированные сервисы в Yandex Cloud:
  - YandexGPT
  - SpeechKit
  - Vision

## Лицензия

Проект создан для образовательных целей.

## Контакты

GitHub: [samsa2846-sys](https://github.com/samsa2846-sys)

