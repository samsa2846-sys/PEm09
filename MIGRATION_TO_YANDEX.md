# 🔄 Миграция проекта на Yandex Cloud API

## ✅ Что было сделано

Проект **полностью адаптирован** для работы как с Yandex Cloud API, так и с OpenAI API.

### Созданные файлы:

1. **services/yandex_client.py** - клиент для Yandex API
   - YandexGPTClient - генерация текста
   - YandexSpeechKitClient - STT/TTS
   - YandexVisionClient - анализ изображений

2. **rag/keyword_search.py** - keyword-based поиск
   - Альтернатива vector embeddings
   - Быстрее и бесплатнее
   - Не требует API для embeddings

3. **rag/index_simple.py** - простой индекс без векторов
   - Хранит чанки в текстовом виде
   - Быстрая индексация
   - Не требует ChromaDB

4. **rag/query_unified.py** - универсальный RAG
   - Автоматически выбирает провайдера
   - Работает с обоими API

### Обновленные файлы:

1. **config.py**
   - Добавлен `API_PROVIDER` (yandex/openai/proxyapi)
   - Добавлены `YANDEX_API_KEY`, `YANDEX_FOLDER_ID`
   - Настройки для Yandex моделей

2. **QUICKSTART.md**
   - Добавлены инструкции для Yandex
   - Выбор провайдера

### Документация:

1. **YANDEX_SETUP.md** - полная инструкция по настройке Yandex
2. **YANDEX_VS_OPENAI.md** - сравнение провайдеров
3. **MIGRATION_TO_YANDEX.md** - этот файл

---

## 🚀 Быстрая миграция (5 минут)

### Шаг 1: Получите Yandex API ключи

1. Зарегистрируйтесь: https://console.cloud.yandex.ru
2. Создайте папку (Folder)
3. Создайте сервисный аккаунт с ролями:
   - `ai.languageModels.user`
   - `ai.speechkit-stt.user` (опционально)
   - `ai.speechkit-tts.user` (опционально)
4. Создайте API ключ
5. Скопируйте:
   - `YANDEX_API_KEY` (AQVN...)
   - `YANDEX_FOLDER_ID` (b1g...)

[Подробная инструкция](YANDEX_SETUP.md)

### Шаг 2: Обновите .env

```env
# Telegram
TELEGRAM_BOT_TOKEN=ваш_токен

# Выбор провайдера
API_PROVIDER=yandex

# Yandex Cloud
YANDEX_API_KEY=AQVN...
YANDEX_FOLDER_ID=b1g...

# OpenAI (оставьте для возможности переключения)
# OPENAI_API_KEY=sk-...
```

### Шаг 3: Установите зависимости

```bash
pip install requests PyPDF2
```

Остальные зависимости уже установлены.

### Шаг 4: Перезапустите бота

```bash
python main.py
```

Вы должны увидеть:
```
INFO - Yandex GPT client initialized with model: yandexgpt-lite
INFO - Simple document index initialized
INFO - Found 1 documents, indexing...
INFO - Indexed 156 document chunks
INFO - Bot started: @ваш_бот
```

### Шаг 5: Протестируйте

```
/start
/mode rag
Какой люк выбрать для магистральной дороги?
```

---

## 🔀 Переключение между провайдерами

### Yandex → OpenAI

```env
API_PROVIDER=openai
OPENAI_API_KEY=sk-...
```

### OpenAI → Yandex

```env
API_PROVIDER=yandex
YANDEX_API_KEY=AQVN...
YANDEX_FOLDER_ID=b1g...
```

### OpenAI → ProxyAPI

```env
API_PROVIDER=proxyapi
OPENAI_API_KEY=sk-or-v1-...
USE_PROXYAPI=true
```

**Перезапустите бота после изменений.**

---

## 📊 Что изменилось в работе

### RAG (поиск по документам)

#### Было (OpenAI):
- Vector embeddings (text-embedding-ada-002)
- ChromaDB для хранения
- Семантический поиск
- Платная индексация (~$1-2)

#### Стало (Yandex):
- Keyword search (поиск по ключевым словам)
- Простой текстовый индекс
- Точный поиск совпадений
- Бесплатная индексация

**Качество:** Keyword search работает хорошо для технических запросов с конкретными терминами.

### Генерация текста

#### Было:
```python
from services.openai_client import openai_client
response = await openai_client.generate_text_response(messages)
```

#### Стало (автоматически):
```python
from rag.query_unified import query_knowledge_base
response = await query_knowledge_base(query, history)
# Провайдер выбирается автоматически из config.API_PROVIDER
```

### Голос (STT/TTS)

#### Было (OpenAI):
- Whisper для STT
- TTS-1 для синтеза
- Формат: MP3

#### Стало (Yandex):
- SpeechKit для STT
- SpeechKit для TTS
- Формат: OGG (нативный для Telegram)

**Преимущество:** Лучше распознает русский язык, дешевле.

---

## 💡 Рекомендации по использованию

### Для максимальной экономии:

```env
API_PROVIDER=yandex
YANDEX_GPT_MODEL=yandexgpt-lite
```

- Стоимость: ~10-20₽ за 100 запросов
- Качество: Достаточно для технических вопросов
- Скорость: Быстро

### Для максимального качества:

```env
API_PROVIDER=openai
GPT_MODEL=gpt-4o
```

- Стоимость: ~300-500₽ за 100 запросов
- Качество: Лучшее
- Скорость: Быстро

### Гибридный подход:

Используйте Yandex для обычных запросов, переключайтесь на OpenAI для сложных:

```python
# В коде можно добавить логику
if is_complex_query(query):
    # Временно используем OpenAI
    pass
```

---

## 🐛 Устранение неполадок

### Проблема: ModuleNotFoundError: services.yandex_client

**Решение:** Убедитесь, что файлы созданы:
```bash
ls services/yandex_client.py
ls rag/keyword_search.py
ls rag/index_simple.py
```

### Проблема: Yandex API error (403)

**Решение:**
1. Проверьте роли сервисного аккаунта
2. Активируйте сервисы в консоли Yandex
3. Убедитесь, что API ключ скопирован полностью

### Проблема: Keyword search не находит документы

**Решение:**
1. Используйте точные термины из каталога
2. Переформулируйте вопрос с ключевыми словами
3. Или переключитесь на OpenAI для семантического поиска:
   ```env
   API_PROVIDER=openai
   ```

### Проблема: Качество ответов хуже

**Решение:** Переключитесь на полную версию:
```env
YANDEX_GPT_MODEL=yandexgpt
```

Или на OpenAI:
```env
API_PROVIDER=openai
```

---

## 📈 Мониторинг использования

### Yandex Cloud

Перейдите: https://console.cloud.yandex.ru/billing

- Текущий баланс
- Использование гранта
- Детализация по сервисам

### Логи бота

```bash
# Смотрите логи в реальном времени
tail -f bot.log

# Или в консоли при запуске
python main.py
```

---

## 🔄 Откат на OpenAI

Если Yandex не подошел, вернитесь к OpenAI:

### Шаг 1: Измените .env

```env
API_PROVIDER=openai
OPENAI_API_KEY=sk-...
```

### Шаг 2: Переиндексируйте (для vector search)

```bash
# Удалите старый индекс
rm -rf data/simple_index/

# Перезапустите (автоматически создаст vector index)
python main.py
```

**Все будет работать как раньше!**

---

## 📚 Дополнительные ресурсы

- [Полная настройка Yandex](YANDEX_SETUP.md)
- [Сравнение провайдеров](YANDEX_VS_OPENAI.md)
- [Быстрый старт](QUICKSTART.md)
- [Тестовые вопросы](TEST_QUESTIONS.md)

---

## ✅ Чек-лист миграции

- [ ] Получены Yandex API ключи
- [ ] Обновлен .env файл
- [ ] Установлены зависимости
- [ ] Бот успешно запущен с Yandex
- [ ] Протестированы базовые функции
- [ ] Проверена статистика индекса (/stats)
- [ ] Протестирован RAG режим
- [ ] Протестирован голосовой режим (опционально)
- [ ] Проверены логи на ошибки
- [ ] Создан бэкап старого .env

---

**Миграция завершена!** 🎉

**Экономия:** ~3000₽ в месяц при активном использовании!

**Следующий шаг:** [Полное тестирование](TEST_QUESTIONS.md)


